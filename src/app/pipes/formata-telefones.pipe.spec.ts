import { FormataTelefonesPipe } from './formata-telefones.pipe';

describe('FormataTelefonesPipe', () => {
  it('create an instance', () => {
    const pipe = new FormataTelefonesPipe();
    expect(pipe).toBeTruthy();
  });
});
